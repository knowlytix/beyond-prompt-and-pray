# Notebook configuration — copy to `.env` and fill in. `.env` is gitignored.
# Load before launching Jupyter:   set -a; source .env; set +a

# ── Licensed GMS backend (the GMS cells in ch.6/8/9 + the capstone) ──
KNOWLYTIX_LICENSE_KEY=
# KNOWLYTIX_INDEX_URL=            # private index, for `pip install knowlytix`
# KNOWLYTIX_EULA_ACCEPTED=1

# ── Optional: an LLM for chapters that call one (any provider) ──
# ANTHROPIC_API_KEY=
